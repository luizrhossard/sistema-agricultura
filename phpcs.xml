<?xml version="1.0"?>
<ruleset name="Laravel App Rules">
    <description>Regras de qualidade para o sistema de agricultura.</description>

    <!-- Pastas analisadas -->
    <file>app</file>
    <file>routes</file>
    <file>database</file>
    <file>tests</file>

    <!-- Mostrar progresso e código das regras -->
    <arg value="p"/>
    <arg value="s"/>

    <!-- Base: PSR-12 -->
    <rule ref="PSR12"/>

    <!-- Exemplos de exclusões opcionais
    <exclude name="Generic.Files.LineLength"/>
    -->
</ruleset>
